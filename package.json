{
  "name": "canvas",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pre-commit-all": "npm-run-all --parallel lint:*",
    "lint:api": "cd api && npm run typecheck && npm run lint",
    "lint:client": "cd client && npm run typecheck && npm run lint",
    "install-all": "npm-run-all --parallel install:*",
    "install:api": "cd api && npm install",
    "install:client": "cd client && npm install",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.3",
    "npm-run-all": "^4.1.5"
  }
}
